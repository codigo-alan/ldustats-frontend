export class HistoricalInfo {
    constructor(velocity, distance, sprints, sprintsDistance, accelerations, decelerations){
        this.velocity = velocity;
        this.distance = distance;
        this.sprints = sprints;
        this.sprintsDistance = sprintsDistance;
        this.accelerations = accelerations;
        this.decelerations = decelerations;
    }
}